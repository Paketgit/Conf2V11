# Вариант №11 
Разработака инструмента для визуализации графа зависимостей для менеджера пакетов.

## Этап 1. Минимальный прототип с конфигурацией
Cоздано минимальное CLI-приложение и сделано настраиваемым.
1. Источником настраиваемых пользователем параметров являются опции
командной строки.
2. К настраиваемым параметрам относятся:
– Имя анализируемого пакета.
– URL-адрес репозитория или путь к файлу тестового репозитория.
– Режим работы с тестовым репозиторием.
– Версия пакета.
– Максимальная глубина анализа зависимостей.
– Подстрока для фильтрации пакетов.
3. (только для этого этапа) При запуске приложения вывести все параметры,
настраиваемые пользователем, в формате ключ-значение.
4. Реализована и продемонстрирована обработка ошибок для всех параметров.
5. Результат выполнения этапа сохранён в репозиторий стандартно
оформленным коммитом.

## Этап 2. Сбор данных
Реализована основная логика получения данных о зависимостях для их
дальнейшего анализа и визуализации.
Требования:
1. Использован формат пакетов Rust (Cargo).
2. Информацию необходимо получить для заданной пользователем версии
пакета.
3. Извлечь информацию о прямых зависимостях заданного пользователем
пакета, используя URL-адрес репозитория.
36
4. (только для этого этапа) Вывести на экран все прямые зависимости
заданного пользователем пакета.
``` 
(venv) PS E:\Conf2V11> python main.py --package syn --repository https://crates.io
  - proc-macro2
  - unicode-ident
  - quote
Найдено зависимостей - 3
```
```
(venv) PS E:\Conf2V11> python main.py --package sdfsdfsdfsy --repository https://1231231
Запрос данных для пакета sdfsdfsdfsy...
Ошибка: Ошибка при получении данных из crates.io: HTTPSConnectionPool(host='1231231', port=443): Max retries exceeded with url: /sdfsdfsdfsy (Caused by NameResolutionError("<urllib3.connection.HTTPSConnection object at 0x000001C215A04ED0>: Failed to resolve '1
231231' ([Errno 11001] getaddrinfo failed)"))
```
```
(venv) PS E:\Conf2V11> python main.py --package sy213n --repository https://crates.io        
Запрос данных для пакета sy213n...
Ошибка: Ошибка при получении данных из crates.io: 404 Client Error: Not Found for url: https://crates.io/sy213n
```

5. Результат выполнения этапа сохранён` в репозиторий стандартно
оформленным коммитом.

## Этап 3. Основные операции
Построен граф зависимостей (с учетом транзитивности) и выполнены
основные операции над ним.
1. Получение графа зависимостей реализован алгоритмом DFS с рекурсией.
2. Проводится анализ с учетом максимальной глубины, заданной
пользователем.
3. Не учитываются при анализе пакеты, имя которых содержит заданную
пользователем подстроку.
4. Корректно обрабатываются случаи наличия циклических зависимостей.
5. Поддерживается режим тестирования. Вместо URL реального репозитория, дать
возможность пользователю указать путь к файлу описания графа
репозитория, где пакеты называются большими латинскими буквами.
Продемонстрировать функциональность этого этапа на различных случаях
работы с тестовым репозиторием.
```
(venv) PS E:\Conf2V11> python main.py --package A --repository test --test-mode --max-depth 3
Тестовый режим для пакета A
Тестовый режим для пакета B                 
Тестовый режим для пакета D                 
Тестовый режим для пакета E                 
Тестовый режим для пакета C                 
Тестовый режим для пакета F                 
                                            
=== Граф зависимостей для A (глубина: 3) ===
A -> ['B', 'C']
B -> ['D', 'E']
D -> []
E -> ['F']
C -> ['F']
F -> []

Циклические зависимости не обнаружены
Обработано пакетов -  6
```
```
(venv) PS E:\Conf2V11> python main.py --package dsfsdf1n345 --repository https://crates.io --max-depth 1              
Запрос данных для пакета dsfsdf1n345...
Ошибка при обработке пакета dsfsdf1n345: Ошибка при получении данных из crates.io: 404 Client Error: Not Found for url: https://crates.io/api/v1/crates/dsfsdf1n345

=== Граф зависимостей для dsfsdf1n345 (глубина: 1) ===
dsfsdf1n345 -> []

Циклические зависимости не обнаружены
```
6. Результат выполнения этапа сохранён в репозиторий стандартно оформленным коммитом.

## Этап 4. Дополнительные операции
Ввыполнены дополнительные операции над графом зависимостей.
1. Поддерживается режим вывода на экран порядка
загрузки зависимостей для заданного пакета. Сравнить результаты с
реальным менеджером пакетов. Если есть расхождения в результатах,
объяснить их наличие.
2. Продемонстрировать функциональность этого этапа на различных случаях
работы с тестовым репозиторием.
```
(venv) PS E:\Conf2V11> python main.py --package F --repository test --test-mode --max-depth 3 --reverse-deps
Обнаружены циклические зависимости:
Цикл: serde -> serde_json -> serde
Обработано пакетов - 19

=== Обратные зависимости для F ===
Пакеты, зависящие от F:
  - E
  - C
Всего обратных зависимостей: 2
```
3. Результат выполнения этапа сохранён в репозиторий стандартно
оформленным коммитом.